CREATE TABLE Birds (
                       id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
                       name TEXT NOT NULL,
                       age INTEGER DEFAULT 0,
                       eyeColor TEXT
);
